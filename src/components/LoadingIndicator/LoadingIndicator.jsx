import React from 'react';
import './LoadingIndicator.css'; // Import the CSS for the loader

const LoadingIndicator = () => (
  <div className="loading-container">
    <p>Loading...</p>
  </div>
);

export default LoadingIndicator;
